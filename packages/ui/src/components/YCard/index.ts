// `components/YCardHeader.vue` and `components/YCardFooter.vue` are fully
// commented-out stubs pointing at paths that no longer exist — not exported.
export { default as YCard } from './YCard.vue'
